package firstprj;

import java.util.Scanner;

public class Main {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		/*Scanner sc = new Scanner(System.in);
		Statistics p1 = null, p2 = null, p3 = null, p4 = null, p5 = null;
		
		for (int i = 0; i < 5; i++) {
			String temp = sc.next();
			// Split the string
			int s = temp.indexOf(",");
			String Firststring = temp.substring(0, s);
			int firstnumber = Integer.parseInt(Firststring);
			String Secondstring = Firststring;
			Secondstring = Firststring.substring(s + 1);
			String Thirdstring = Secondstring;
			Thirdstring = Secondstring.substring(s + 2);
			
			switch(i) {
				case 0:
					p1 = new Statistics(temp, 0, 0, 0); // insert the values
					break;
				case 1:
					p2 = new Statistics(temp, 0, 0, 0); // insert the values
					break;
				case 2:
					p3 = new Statistics(temp, 0, 0, 0); // insert the values
					break;
				case 3:
					p4 = new Statistics(temp, 0, 0, 0); // insert the values
					break;
				case 4:
					p5 = new Statistics(temp, 0, 0, 0); // insert the values
					break;
				default:
					break;
			}
		}
		
		System.out.println(howManyEmpty(p1, p2, p3, p4, p5));
		System.out.println(p1.getName());
		System.out.println(p2.getName());
		System.out.println(p3.getName());
		System.out.println(p4.getName());
		System.out.println(p5.getName());
		
	}
	
	public static int howManyEmpty(Statistics p1, Statistics p2, Statistics p3, Statistics p4, Statistics p5) {
		int ret = 5;
		if (p1 != null) {
			ret--;
		}
		if (p2 != null) {
			ret--;
		}
		if (p3 != null) {
			ret--;
		}
		if (p4 != null) {
			ret--;
		}
		if (p5 != null) {
			ret--;
		}
		
		return ret; */
		

						
		/* Printclass라는 클래스를 불러오는 작업: new 뒤에오는 것과 이름 일치해야 함
		   printclass2: Main 클래스에 만드는 새로운 Method */
		
		/*System.out.println(printclass2.printAdd(1, 2));
		System.out.println(printclass2.printSub(3, 2));
		System.out.println(printclass2.printMulti(1, 2));
		System.out.println(printclass2.printDivi(4, 2)); */
		
		/* Printclass.printAdd(2, 3); 
		Printclass에 static을 추가한 후 sysout을 걸면 main 함수에서 Printclass.만 써도 함수가 드래그에 뜸 */
		
		/* Test test = new Test(5, "30");
		System.out.println(test.getA() + test.getB()); */		
	}
}

